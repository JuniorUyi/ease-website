import * as React from 'react';
import styles from '../styles/styles.scss';

// styles

// data

// markup
const IndexPage = () => (
  
    <main>
      <title>Home Page</title>
      <h1>I'm the layout page</h1>
    </main>
  
);

export default IndexPage;
